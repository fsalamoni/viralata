/**
 * Stub mínimo de `firebase-functions` (entry point) para os testes.
 * O `logger` é um no-op silencioso.
 */
module.exports = {
  logger: {
    info: () => {},
    warn: () => {},
    error: () => {},
    debug: () => {},
  },
};
