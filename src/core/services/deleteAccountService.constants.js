/**
 * @fileoverview Constantes do deleteAccountService.
 * Exportadas separadamente para serem testáveis.
 */

/** Nome placeholder para usuários que deletaram a conta. */
export const ANONYMIZED_NAME = '[conta excluída]';

/** Motivo registrado no `left_reason` de memberships e rostagem. */
export const LEFT_REASON_ACCOUNT_DELETED = 'account_deleted';
